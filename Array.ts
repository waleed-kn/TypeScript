const SuperPower: string[] = [];
const Power: number[] = [];
Power.push(2);
SuperPower.push(" i dont have the superpower");
type user = {
    name: string
    email: string;
}
const allUser: user[] = [];
allUser.push({ name: "Waleed", email: "waleedLinkedin@" });